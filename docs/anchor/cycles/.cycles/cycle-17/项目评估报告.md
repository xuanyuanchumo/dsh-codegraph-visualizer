# Cycle-17 项目评估报告

## 评估日期: 2026-09-03

## 评估基线

- TypeScript 类型检查: 全部通过 (host/client/test 三面零错误)
- 单元测试: 284 个测试全部通过 (10 个测试文件)
- 代码规模: ~40 个 TS/TSX 源文件

## 评估维度 E1-E8

### E1 功能覆盖

- 已实现: FR-01~FR-11 (图谱渲染、多数据源、类型切换、节点交互、实时更新、搜索高亮、布局切换、调用链、过滤器、导出)
- 已实现: FR-12 循环依赖检测 (detectCycles + highlightCycles)
- 已实现: 分层节点展示 (depthLevel 1/2/3 + expandNode/collapseNode)
- 已实现: 工作区选择器 (WorkspaceSelector + DSH workspace 同步)
- 已实现: 前置插件检测与安装引导 (EmptyState + ImportPanel)
- 缺口: FR-13 影响分析可视化 (graph_impact 工具存在但无 UI 展示)
- 缺口: FR-14 继承层次图、FR-15 智能聚类、FR-16 缩略图导航
- 缺口: FR-17 键盘快捷键 (KeyboardHelp 组件存在但功能不完整)

### E2 架构健康

- Host/Client 隔离: 合规 (HTTP 路由通信，无跨边界直接引用)
- 能力缝三角色: 合规 (IGraphVisualizerService → GraphVisualizerService → Consumer)
- 依赖方向: 合理 (Client → HTTP → Host → tools → adapters → merger)
- Branded ID: 合规 (NodeId/EdgeId/RepoId/SymbolId 均为 Branded 类型)
- `as never` 断言: 已清除 (Cycle-16 的 C1-C3 已修复)
- `as unknown as` 双重断言: 9处，均为合理的类型边界转换:
  - `ctx as unknown as ContextWithSessions` (4处): Cordis Context 扩展，声明合并未覆盖 workspaceRegistry
  - `node:sqlite` 返回类型 (2处): Node.js 实验性 API 无类型声明
  - `JsonValue` 序列化 (2处): DSH 工具链类型边界
  - `globalThis.__CG_DEBUG` (1处): 运行时全局变量检测

### E3 技术债务

- `as unknown as` 双重断言: 9处 — 可接受 (均为类型边界转换，非逻辑绕过)
- HTTP 返回数据运行时验证: `validators.ts` 已实现 `validateGraphData`，但 `fetchGraphData` 在 `client/index.ts` 中使用，部分路径未经验证
- 路径安全: `isPathAllowed` 已实现路径白名单校验
- i18n replace 只替换第一个: 需修复 (L6 from Cycle-16)

### E4 测试成熟度

- 284 个单元测试全部通过
- 覆盖率: 需重新测量 (Cycle-16 报告 lines/statements 79%, functions 75%)
- E2E 测试文件存在: filter-and-export, user-journey, search-and-analysis, integration-and-lifecycle, chaos, integration, realtime, large-repo, local-repos
- 用户旅程 J1-J14: E2E 测试文件覆盖

### E5 性能与稳定

- 分层渲染: 已实现 (depthLevel + expandNode/collapseNode)
- 大图谱优化: 已实现 (批量添加 + requestAnimationFrame + 自动降级布局)
- Loading failsafe: 已实现 (15秒超时自动清除 isLoading)
- 轮询间隔: watchEnabled 时 3s，否则 10s — 合理

### E6 安全与可访问

- 路径注入: `isPathAllowed` 白名单校验已实现
- XSS: Cytoscape.js 渲染不涉及 HTML 注入
- a11y: ARIA 标签覆盖 (role/aria-label/aria-pressed/aria-expanded)
- 主题适配: DSH dark/light 主题检测 (MutationObserver)

### E7 创新差异化

- 多数据源融合: 已实现 (CodeGraph + Lens → GraphDataMerger)
- 实时图谱渲染: 已实现 (文件监听 + 轮询降级 + ETag 增量更新)
- 分层节点展示: 已实现 (depthLevel 1/2/3 + 双击展开)
- 工作区同步: 已实现 (DSH workspace 自动检测 + 手动切换)

### E8 多源对照增强

- 对照主流工具 (CodeSee/Understand): 分层展示已对齐
- 实时更新: 超越多数静态分析工具

## 优先修复/增强项

| 优先级 | 编号 | 问题/需求 | 影响 |
|--------|------|-----------|------|
| High | F-01 | i18n replace 只替换第一个匹配 | i18n 质量 |
| High | F-02 | 前置插件安装命令需加 --profile web | 用户体验 |
| High | F-03 | 工作区名称可视化显示优化 | 用户体验 |
| Medium | F-04 | ErrorBoundary 不响应语言切换 | a11y/i18n |
| Medium | F-05 | GraphPanel 选择器优化 (useShallow 已用) | 性能 |
| Medium | F-06 | CSS 现代化与响应式布局优化 | UI 质量 |
| Low | F-07 | 合并策略 last-write-wins | 数据完整性 |

## Cycle-17 目标

1. 修复 i18n replace 全局替换
2. 优化前置插件安装命令 (加 --profile web)
3. 增强工作区名称显示
4. CSS 现代化与响应式布局优化
5. 增强用户旅程覆盖
6. DSH 真实环境验证