# Cycle-12 当前循环总规划

| 项目 | 内容 |
|------|------|
| **循环** | cycle-12 |
| **日期** | 2026-08-30 |
| **模式** | L4 全自主（P13） |
| **范围声明** | 缺陷修复（P0×4）+ 性能/健壮性加固（P1×3）+ 功能增强（P2×3）+ 用户旅程深度验证 |

---

## 一、输入与依据

1. `docs/需求分析.md` v2.0（J1–J14、FR/CR/NFR 清单）
2. `docs/开发计划.md`（Phase 0–6、发布前检查清单）
3. `docs/测试.md`（四层测试架构、覆盖率门限、dsh-testkit / dsh-eval-harness 工作流）
4. `docs/anchor/cycles/项目迭代驱动验证流程.md` v2.0（十九原则、九条红线、G0–G3 门禁）
5. DeepSeek Harness 官方文档（development.zh.md / architecture 规范）
6. React Testing / React Patterns / Vercel React Best Practices / ponytail 技能
7. cycle-11 测试报告（65/65 单测、9/9 E2E 全绿基线）

## 二、代码审查发现（本循环立项依据）

| # | 等级 | 缺陷 | 位置 | 违反 |
|---|------|------|------|------|
| D1 | P0 | 假轮询：`usePolling` 仅 `setLoading(true)` 无复位路径，loading overlay 可能永久显示；`setGraphData` 不重置 `isLoading` | GraphPanel.tsx / graphStore.ts | FR-05、NFR-05 |
| D2 | P0 | `usePolling` 内联 callback 每渲染重建 interval（rerender 级联） | hooks/index.ts | vercel rerender 规则 |
| D3 | P0 | `window.addEventListener` 泄漏：`apply()` 内原生监听器无 dispose（J13 红线 1） | client/index.ts | 红线 1（注册即 effect）、J13 |
| D4 | P0 | Cytoscape selector 注入：`cy.edges(`[source="${id}"]`)` 于含特殊字符 ID 时选择器被破坏/注入 | CytoscapeRenderer.ts:249,274 | NFR-07 安全 |
| D5 | P1 | `detectCycles` 递归 DFS 大图爆栈（10000 节点目标） | CytoscapeRenderer.ts | NFR-01 |
| D6 | P1 | `highlightCallChain` BFS `shift()` O(n²) | CytoscapeRenderer.ts | 性能门禁 |
| D7 | P1 | `graphCache` 无上限，长会话内存增长 | tools.ts | NFR-06 |
| D8 | P2 | 主题/布局/过滤不持久化 | graphStore.ts | FR-10/J10 |
| D9 | P2 | 搜索无结果时无空状态反馈 | GraphPanel.tsx | J3 反向场景 |
| D10 | P2 | dev-server 加载 inline 复制品而非真实 client bundle（架构保真缺口） | dev-server.mjs | 旅程验证有效性 |

## 三、任务拆分

| 层 | 任务 | 绑定测试 |
|----|------|---------|
| Client UI | D1/D2/D5/D6/D8/D9 | vitest（graphStore/renderer/hooks） |
| Host 服务 | D7 LRU | vitest（tools.test.ts） |
| 契约/生命周期 | D3 disposer 收集 | vitest（plugin.test.ts） |
| 架构保真 | D10 真实 bundle 优先加载 | dev-server + chrome-devtools MCP |
| E2E | 用户旅程（弹窗→入口→布局/主题/搜索/过滤/导出/mini-map/折叠） | chrome-devtools MCP 实测 |

## 四、门禁（G0–G3）

| 门禁 | 判定 |
|------|------|
| G0 评估 | 审查发现 D1–D10 登记（本文件） |
| G1 typecheck | host+client+test 三面零错误 |
| G2 lint | oxlint 零警告 |
| G3 测试 | vitest 100% 通过 + chrome-devtools 旅程通过 |

## 五、数据清理纪律（P10）

- 验证后关闭 dev-server 进程
- 不提交临时验证产物