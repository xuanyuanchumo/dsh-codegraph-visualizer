# 循环子规划 Template

> 来源：`docs/anchor/cycles/项目迭代驱动验证流程.md` §5.3
> 命名：`cycle-{N}-subplan-{type}-{name}.md`（type ∈ feature / fix / complex；依赖升级为 `complex-upgrade-*`，流程见附录 D §2）；置于循环工作区 `docs/anchor/cycles/.cycles/cycle-{N}/subplans/`。
> **硬性要求**：每个子规划必须（1）绑定至少一个测试用例 ID；（2）包含文档变更验证项。

## 1. 元信息

| 字段       | 值                                |
| ---------- | --------------------------------- |
| 子规划命名 | `cycle-{N}-subplan-{type}-{name}` |
| 归属循环   | `cycle-{N}`                       |
| 类型       | feature / fix / complex           |
| 执行者     | AI / 人工                         |

## 2. 目标与验收标准

- 目标：（一句话可验证）
- 关联测试用例 ID：`TC-xxx`、`TC-yyy`（必须 ≥1；推荐 Client UI / Host / 契约各按涉及层覆盖，见附录C §3 RTM）
- 验收标准（对齐附录A G0/G1/G2/G3）

## 3. 涉及文件 / 接口 / 测试修改

| 类型     | 路径 / 名称                            | 变更说明 |
| -------- | -------------------------------------- | -------- |
| 源文件   |                                        |          |
| 接口     |                                        |          |
| 测试     |                                        |          |
| 文档变更 | （本子规划改动涉及的文档，须纳入验证） |          |

## 4. 依赖的工具 / 技能

- 工具：codegraph / codebase-memory / pnpm run test·typecheck·build / Playwright（E2E）
- 说明：…

## 5. 运行时问题记录

| 问题描述 | 根因候选 | 影响范围 | 处理 | 是否登记任务清单 |
| -------- | -------- | -------- | ---- | ---------------- |
|          |          |          |      |                  |

## 6. 完成标准

- 关联全部测试用例通过（含文档变更项）
- 涉及数据转换/搜索/缓存/图谱的变更已附复杂度说明与基准数据（附录 D §5.3）
- 无未登记遗留 TODO
- 收敛门禁（附录A G0/G1/G2/G3）满足

> 复杂问题可独立生成「问题解决子规划」，完成后返回上级并转接，标准与主循环一致（100% 验证通过）。
