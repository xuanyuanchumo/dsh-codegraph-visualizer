# 任务清单 Template

> 来源：`docs/anchor/cycles/项目迭代驱动验证流程.md` §5（循环工作区）+ 附录A §3.4
> 命名：`任务清单.md`；置于循环工作区 `docs/anchor/cycles/.cycles/cycle-{N}/`。P0-P3 分级 + 拓扑排序（依赖在前）。每行须标注**归属层（Client UI / Host / 契约）**。全部完成（含文档项）才可标记 completed。

## 1. 头部

| 循环编号    | 日期 | 执行者 | 关联测试报告 |
| ----------- | ---- | ------ | ------------ |
| `cycle-{N}` |      |        |              |

## 2. P0（核心：阻塞性 / 数据丢失 / 崩溃）

| 任务 | 归属层(Client UI/Host/契约) | 关联用例 ID | 文档变更项 | 依赖 | 状态 |
| ---- | ---------------------- | ----------- | ---------- | ---- | ---- |
|      |                        |             |            |      | 待办 |

## 3. P1（重要功能）

| 任务 | 归属层(Client UI/Host/契约) | 关联用例 ID | 文档变更项 | 依赖 | 状态 |
| ---- | ---------------------- | ----------- | ---------- | ---- | ---- |
|      |                        |             |            |      | 待办 |

## 4. P2（一般功能/体验）

| 任务 | 归属层(Client UI/Host/契约) | 关联用例 ID | 文档变更项 | 依赖 | 状态 |
| ---- | ---------------------- | ----------- | ---------- | ---- | ---- |
|      |                        |             |            |      | 待办 |

## 5. P3（建议/低优先级）

| 任务 | 归属层(Client UI/Host/契约) | 关联用例 ID | 文档变更项 | 依赖 | 状态 |
| ---- | ---------------------- | ----------- | ---------- | ---- | ---- |
|      |                        |             |            |      | 待办 |

## 6. 流转 / 登记项

| 来源 | 项描述 | 归属层 | 去向（下循环/登记） | 门禁影响 |
| ---- | ------ | ------ | ------------------- | -------- |
|      |        |        |                     |          |

## 7. 状态约定

- `待办 → 进行中 → 完成`；`完成` 需关联用例 + 文档项全部通过，否则保持非完成态。
- G1 判定以本清单全部 completed 为准（不得有未登记遗留 TODO）。
